# Beszel-Agent for Bazzite
> You need an already running Beszel Monitor
This is for monitoring from external machine the CPU, RAM and GPU utilization.

Note : Podman is by default installed on Bazzite to manage Dockerfiles, thus I'm using this tool and that means **I don't know if that scripts works on the Bazzite's fork for devs**

> This Beszel-agent script might not support iGPU. Again if you give me a handheld machine I might look to work on supporting that